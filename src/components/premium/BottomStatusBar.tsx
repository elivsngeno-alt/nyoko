import { useEffect, useState } from 'react';
import { PlayIcon } from './icons';
const formatUTC=(d:Date)=>{const p=(v:number)=>String(v).padStart(2,'0');return `${d.getUTCFullYear()}-${p(d.getUTCMonth()+1)}-${p(d.getUTCDate())} ${p(d.getUTCHours())}:${p(d.getUTCMinutes())}:${p(d.getUTCSeconds())} GMT`;};
const BottomStatusBar=()=>{const[now,setNow]=useState(new Date());useEffect(()=>{const t=window.setInterval(()=>setNow(new Date()),1000);return()=>window.clearInterval(t);},[]);return <div className='prodb-bottom-bar'><button className='prodb-risk'>Risk Disclaimer</button><div className='prodb-run-status'><button className='prodb-run'><PlayIcon /> Run</button><div className='prodb-execution'><small>EXECUTION</small><strong>FAST</strong><span className='prodb-switch'><i /></span></div><div className='prodb-bot-state'><strong>Bot is not running</strong><span /></div></div><div className='prodb-bottom-meta'><i className='prodb-online-dot' /><span>{formatUTC(now)}</span><button>☼</button><button>⇥</button><button>⛶</button></div></div>;};
export default BottomStatusBar;
